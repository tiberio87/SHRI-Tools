version: "3.8"

services:
  shri-tools:
    build: .
    container_name: shri-tools
    ports:
      - "5901:5901"
      - "6080:6080"
    volumes:
      - ./data:/data
      - ./media:/media
      - ./output:/output
      - /tmp/.X11-unix:/tmp/.X11-unix
    restart: unless-stopped
